<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Премиум Отель | Комфорт и Яркость</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;800&display=swap" rel="stylesheet">
    <style>
        /* Сброс стилей */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Montserrat', sans-serif;
        }

        :root {
            --primary-dark: #1A1A2E;
            --primary-blue: #16213E;
            --accent-color: #FF6B6B;
            --accent-hover: #FF4757;
            --light-bg: #F7F9FC;
            --text-dark: #333333;
            --text-light: #FFFFFF;
            --gold: #FCA311;
        }

        body {
            background-color: var(--light-bg);
            color: var(--text-dark);
            line-height: 1.6;
        }

        /* Навигация */
        header {
            background-color: var(--primary-dark);
            padding: 20px 5%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
            box-shadow: 0 4px 20px rgba(0,0,0,0.1);
        }

        .logo {
            color: var(--text-light);
            font-size: 28px;
            font-weight: 800;
            text-decoration: none;
            letter-spacing: 1px;
        }

        .logo span {
            color: var(--accent-color);
        }

        nav ul {
            list-style: none;
            display: flex;
            gap: 30px;
        }

        nav ul li a {
            color: var(--text-light);
            text-decoration: none;
            font-weight: 600;
            transition: color 0.3s ease;
        }

        nav ul li a:hover {
            color: var(--gold);
        }

        .btn-book-header {
            background-color: var(--accent-color);
            color: var(--text-light);
            padding: 12px 25px;
            border-radius: 30px;
            text-decoration: none;
            font-weight: 800;
            transition: background 0.3s ease, transform 0.2s ease;
            box-shadow: 0 4px 15px rgba(255, 107, 107, 0.4);
        }

        .btn-book-header:hover {
            background-color: var(--accent-hover);
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(255, 107, 107, 0.6);
        }

        /* Главный экран (Hero) */
        .hero {
            height: 100vh;
            background: linear-gradient(rgba(26, 26, 46, 0.8), rgba(22, 33, 62, 0.9)), url('hero-bg.jpg') center/cover;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 0 20px;
            margin-top: 0;
        }

        .hero h1 {
            color: var(--text-light);
            font-size: 60px;
            font-weight: 800;
            margin-bottom: 20px;
            text-shadow: 2px 2px 10px rgba(0,0,0,0.5);
            max-width: 900px;
        }

        .hero h1 span {
            color: var(--gold);
        }

        .hero p {
            color: #E0E0E0;
            font-size: 22px;
            margin-bottom: 40px;
            max-width: 700px;
        }

        .btn-main {
            background-color: var(--gold);
            color: var(--primary-dark);
            padding: 18px 45px;
            font-size: 20px;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 800;
            transition: all 0.3s ease;
            box-shadow: 0 10px 30px rgba(252, 163, 17, 0.5);
        }

        .btn-main:hover {
            background-color: #FFB732;
            transform: translateY(-5px);
            box-shadow: 0 15px 40px rgba(252, 163, 17, 0.7);
        }

        /* Преимущества */
        .features {
            padding: 100px 5%;
            text-align: center;
            background-color: var(--light-bg);
        }

        .section-title {
            font-size: 40px;
            color: var(--primary-dark);
            margin-bottom: 60px;
            font-weight: 800;
            position: relative;
            display: inline-block;
        }

        .section-title::after {
            content: '';
            position: absolute;
            width: 50%;
            height: 4px;
            background-color: var(--accent-color);
            bottom: -15px;
            left: 25%;
            border-radius: 2px;
        }

        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 40px;
            margin-top: 40px;
        }

        .feature-card {
            background: #ffffff;
            padding: 40px 30px;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.05);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            position: relative;
            overflow: hidden;
            border-bottom: 5px solid var(--accent-color);
        }

        .feature-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
        }

        .feature-icon {
            font-size: 50px;
            margin-bottom: 20px;
        }

        .feature-card h3 {
            font-size: 24px;
            color: var(--primary-blue);
            margin-bottom: 15px;
        }

        .feature-card p {
            color: #666;
            font-size: 16px;
        }

        /* Номера */
        .rooms {
            padding: 100px 5%;
            background-color: var(--primary-blue);
            text-align: center;
        }

        .rooms .section-title {
            color: var(--text-light);
        }

        .rooms-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 40px;
            margin-top: 60px;
        }

        .room-card {
            background: #ffffff;
            border-radius: 20px;
            overflow: hidden;
            text-align: left;
            box-shadow: 0 15px 40px rgba(0,0,0,0.2);
            transition: transform 0.3s ease;
        }

        .room-card:hover {
            transform: scale(1.03);
        }

        .room-img {
            width: 100%;
            height: 250px;
            background-color: #ddd;
            background-position: center;
            background-size: cover;
        }

        .img-1 { background-image: url('room-1.jpg'); }
        .img-2 { background-image: url('room-2.jpg'); }
        .img-3 { background-image: url('room-3.jpg'); }

        .room-info {
            padding: 30px;
        }

        .room-title {
            font-size: 26px;
            color: var(--primary-dark);
            margin-bottom: 10px;
            font-weight: 800;
        }

        .room-price {
            font-size: 22px;
            color: var(--accent-color);
            font-weight: 800;
            margin-bottom: 15px;
        }

        .room-desc {
            color: #666;
            margin-bottom: 25px;
            font-size: 15px;
        }

        .btn-book-room {
            display: block;
            text-align: center;
            background-color: var(--primary-dark);
            color: var(--text-light);
            padding: 15px;
            border-radius: 10px;
            text-decoration: none;
            font-weight: 600;
            transition: background 0.3s ease;
        }

        .btn-book-room:hover {
            background-color: var(--accent-color);
        }

        /* Футер */
        footer {
            background-color: var(--primary-dark);
            color: var(--text-light);
            padding: 60px 5% 30px;
        }

        .footer-content {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 40px;
            margin-bottom: 40px;
        }

        .footer-col h4 {
            font-size: 22px;
            color: var(--gold);
            margin-bottom: 20px;
        }

        .footer-col p {
            color: #ccc;
            margin-bottom: 10px;
            font-size: 16px;
        }

        .footer-col a {
            color: #ccc;
            text-decoration: none;
            transition: color 0.3s;
        }

        .footer-col a:hover {
            color: var(--accent-color);
        }

        .footer-bottom {
            text-align: center;
            padding-top: 30px;
            border-top: 1px solid rgba(255,255,255,0.1);
            color: #888;
        }

        /* Адаптивность для мобильных */
        @media (max-width: 768px) {
            nav ul {
                display: none;
            }
            .hero h1 {
                font-size: 40px;
            }
            .hero p {
                font-size: 18px;
            }
            .section-title {
                font-size: 32px;
            }
        }
    </style>
</head>
<body>

    <header>
        <a href="#" class="logo">ГРАНД<span>УЮТ</span></a>
        <nav>
            <ul>
                <li><a href="#features">Преимущества</a></li>
                <li><a href="#rooms">Номера</a></li>
                <li><a href="#contacts">Контакты</a></li>
            </ul>
        </nav>
        <a href="#rooms" class="btn-book-header">Забронировать</a>
    </header>

    <section class="hero">
        <h1>Ваш идеальный отдых <span>начинается здесь</span></h1>
        <p>Забудьте о стандартах. Погрузитесь в атмосферу абсолютного комфорта, современного дизайна и безупречного сервиса.</p>
        <a href="#rooms" class="btn-main">Выбрать лучший номер</a>
    </section>

    <section id="features" class="features">
        <h2 class="section-title">Почему выбирают нас</h2>
        <div class="features-grid">
            <div class="feature-card">
                <div class="feature-icon">✨</div>
                <h3>Новый формат</h3>
                <p>Свежий, современный ремонт и продуманный до мелочей дизайн, который радует глаз и создает настроение.</p>
            </div>
            <div class="feature-card">
                <div class="feature-icon">🛏️</div>
                <h3>Премиум комфорт</h3>
                <p>Ортопедические матрасы, белоснежное белье и идеальная чистота в каждом номере нашей гостиницы.</p>
            </div>
            <div class="feature-card">
                <div class="feature-icon">📍</div>
                <h3>В центре событий</h3>
                <p>Удобное расположение, парковка и быстрый доступ ко всем ключевым точкам города.</p>
            </div>
        </div>
    </section>

    <section id="rooms" class="rooms">
        <h2 class="section-title">Наши номера</h2>
        <div class="rooms-grid">
            <div class="room-card">
                <div class="room-img img-1"></div>
                <div class="room-info">
                    <h3 class="room-title">Стандарт Комфорт</h3>
                    <div class="room-price">от 2 500 ₽ / сутки</div>
                    <p class="room-desc">Идеальный баланс цены и качества. Просторная кровать, рабочая зона, скоростной Wi-Fi и собственная ванная комната с тропическим душем.</p>
                    <a href="#contacts" class="btn-book-room">Забронировать сейчас</a>
                </div>
            </div>

            <div class="room-card">
                <div class="room-img img-2"></div>
                <div class="room-info">
                    <h3 class="room-title">Бизнес Студия</h3>
                    <div class="room-price">от 3 800 ₽ / сутки</div>
                    <p class="room-desc">Улучшенный номер с панорамными окнами. Дополнительная зона отдыха, кофемашина и премиальная косметика для вашего идеального утра.</p>
                    <a href="#contacts" class="btn-book-room">Забронировать сейчас</a>
                </div>
            </div>

            <div class="room-card">
                <div class="room-img img-3"></div>
                <div class="room-info">
                    <h3 class="room-title">Президентский Люкс</h3>
                    <div class="room-price">от 6 000 ₽ / сутки</div>
                    <p class="room-desc">Максимум пространства и роскоши. Двухкомнатный номер с огромной King-size кроватью, джакузи и эксклюзивным обслуживанием в номер.</p>
                    <a href="#contacts" class="btn-book-room">Забронировать сейчас</a>
                </div>
            </div>
        </div>
    </section>

    <footer id="contacts">
        <div class="footer-content">
            <div class="footer-col">
                <h4>ГРАНД УЮТ</h4>
                <p>Мы переосмыслили понятие гостеприимства. Ваш отдых — наш главный приоритет.</p>
            </div>
            <div class="footer-col">
                <h4>Контакты</h4>
                <p>📍 ул. Идеальная, 1, Центр</p>
                <p>📞 <a href="tel:+79000000000">+7 (900) 000-00-00</a></p>
                <p>✉️ <a href="mailto:booking@grand-uyut.ru">booking@grand-uyut.ru</a></p>
            </div>
            <div class="footer-col">
                <h4>Режим работы</h4>
                <p>Круглосуточное заселение</p>
                <p>Без выходных и перерывов</p>
            </div>
        </div>
        <div class="footer-bottom">
            <p>&copy; 2026 Гранд Уют. Все права защищены. Сайт создан для абсолютного лидерства.</p>
        </div>
    </footer>

    <script>
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                document.querySelector(this.getAttribute('href')).scrollIntoView({
                    behavior: 'smooth'
                });
            });
        });
    </script>
</body>
</html>
